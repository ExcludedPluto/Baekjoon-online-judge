알고리즘
	1. 매초마다 처음에 검사 아니면 밑 실행
	2. R연산 기준으로 설명.
		-한 행을 앞에서부터 검사 - temp 배열을 선언하여 나온 수만큼 기록함
		-temp배열에 0이 아니면, 1개라도 있는 것. temp를 앞에서 부터 검사하여 vector<Cell> 에 값과 나온 수를 저장.
		-벡터를 정렬 -> Cell 구조체 안에 정렬 함수를 넣음
		-벡터를 앞에서부터 꺼내서 다시 원래 행에 집어 넣음
			-이때 따로 index를 선언하여 집어 넣고, 다 끝난 다음에 원래 행의 길이보다 짧으면, 남은 만큼 0으로 초기화
		-index가 maxWidth보다 크면 maxWidth에 저장.
		-모든 행이 끝나고 width에 maxWidth를 저장.

 